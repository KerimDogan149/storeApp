<nav class="navbar navbar-expand-lg bg-white shadow-sm border-bottom">
    <div class="container d-flex align-items-center">

        <!-- LOGO -->
        <a class="navbar-brand d-flex align-items-center me-4" href="/">
            <i class="bi bi-basket text-primary fs-3 me-2"></i>
            <span class="fw-bold fs-4 text-primary">StoreApp</span>
        </a>

        <!-- TOGGLER -->
        <button class="navbar-toggler ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain"
                aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- MENÜLER -->
        <div class="collapse navbar-collapse justify-content-between" id="navbarMain">
            <ul class="navbar-nav mx-auto mb-2 mb-lg-0">

                <!-- Tüm Kategoriler Menüsü -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle fw-bold text-dark" href="#" id="tumKategorilerDropdown" role="button"
                       data-bs-toggle="dropdown" aria-expanded="false">
                        Tüm Kategoriler
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="tumKategorilerDropdown">
                        @await Component.InvokeAsync("NavbarAllCategories")
                    </ul>
                </li>

                <!-- Öne çıkan birkaç kategori -->
                <vc:navbar-top-categories></vc:navbar-top-categories>

            </ul>

            <!-- Sağ Kısım: Arama + Sepet -->
            <ul class="navbar-nav d-flex align-items-center">
                <li class="nav-item me-3">
                    <form method="get" asp-controller="Home" asp-action="Search" class="d-flex">
                        <input class="form-control form-control-sm me-2" type="search" name="q" placeholder="Ürün ara..." aria-label="Search">
                        <button class="btn btn-outline-dark btn-sm" type="submit">
                            <i class="bi bi-search"></i>
                        </button>
                    </form>
                </li>

                <li class="nav-item">
                    <vc:cart-summary></vc:cart-summary>
                </li>
            </ul>
        </div>
    </div>
</nav>
