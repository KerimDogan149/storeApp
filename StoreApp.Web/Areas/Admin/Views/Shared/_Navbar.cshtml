<nav class="navbar navbar-expand-lg bg-white shadow-sm border-bottom mb-4">
    <div class="container d-flex justify-content-between align-items-center">

        <!-- LOGO -->
        <a class="navbar-brand fw-bold text-primary fs-4" href="/Admin/Admin">
            <i class="bi bi-speedometer2 me-2"></i> StoreApp Admin Paneli
        </a>

        @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
        {
            <!-- Menü (Sadece Admin Girişliyse Görünsün) -->
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item mx-2">
                    <a class="nav-link" asp-area="Admin" asp-controller="Category" asp-action="Index">
                        <i class="bi bi-tags me-1"></i> Kategoriler
                    </a>
                </li>

                <li class="nav-item mx-2">
                    <a class="nav-link" asp-area="Admin" asp-controller="Product" asp-action="Index">
                        <i class="bi bi-box-seam me-1"></i> Ürünler
                    </a>
                </li>

                <li class="nav-item mx-2">
                    <a class="nav-link" asp-area="Admin" asp-controller="Slide" asp-action="Index">
                        <i class="bi bi-images me-1"></i> Slaytlar
                    </a>
                </li>

                <li class="nav-item mx-2">
                    <a class="nav-link" asp-area="Admin" asp-controller="Campaign" asp-action="Index">
                        <i class="bi bi-megaphone me-1"></i> Kampanyalar
                    </a>
                </li>

                <li class="nav-item mx-2">
                    <a class="nav-link" asp-area="Admin" asp-controller="SiteSocialSettings" asp-action="SiteSocialSettings">
                        <i class="bi bi-share-fill me-1"></i> Sosyal Ayarlar
                    </a>
                </li>

                                <li class="nav-item mx-2">
                    <a class="nav-link" asp-area="Admin" asp-controller="User" asp-action="Index">
                        <i class="bi bi-people me-1"></i> Kullanıcılar
                    </a>
                </li>
            </ul>
        }


        <!-- Sağ Kısım -->
        <div class="d-flex align-items-center gap-2 ">
                    @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
                {
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle btn btn-sm btn-outline-white rounded-pill px-3" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-person-circle me-1"></i> Hesabım
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow">

                            <li>
                                <form asp-area="Admin" asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                                    <button type="submit" class="dropdown-item text-danger">
                                        <i class="bi bi-box-arrow-right me-2"></i> Çıkış Yap
                                    </button>
                                </form>
                            </li>
                        </ul>
                    </li>
                }

            else
            {
                    <li class="nav-item">
                        <a class="btn btn-outline-primary btn-sm rounded-pill me-2" asp-area="Admin" asp-controller="Account" asp-action="Login">Giriş Yap</a>
                    </li>
            }
        </div>

    </div>
</nav>
